## v1.5.51

- UX: Im Modus "Salzwasser" kein "Chlor hinzufügen" mehr; stattdessen Hinweis zur Salzumwandlung.
- UX: Im Modus "Mischbetrieb" smarter Hinweis (Salzumwandlung prüfen oder Chlor dosieren).

## v1.5.50

- Bugfix: Frost-Icon öffnet More-Info für "Frostgefahr" (statt Laufgrund).

## v1.5.49

- Feature: Wartung zeigt "Salz hinzufügen" (auto-discovered via `*_salt_add_g`).

## v1.5.48

- UX: Status icons repositioned to sit centered between power badge and current temperature.

## v1.5.47

- UX: Power badge moved further inside the ring and made smaller.
- UX: Current temperature font size reduced a bit more.

## v1.5.46

- Bugfix: Mid-icon now opens More-Info reliably (falls back to climate entity if run/heat reason sensors are not available).
- UX: Dial spacing tweaks (power badge no longer overlaps icons; slightly smaller current temperature).
- UX: Upcoming rows aligned (title left, value right, shown as "<n> min").

## v1.5.45

- Feature: Show outdoor temperature in the dial (auto-discovered).
- Feature: Show “next frost protection run in …” countdown (auto-discovered).
- UX: Move power display into a badge above the dial icons.

## v1.5.43

- Bugfix: Countdown bis zum nächsten Kalender-Termin bleibt korrekt (kein "0" mehr nur wegen überlappender/ganztägiger Events).

## 🐛 KRITISCHER BUGFIX: Auto-Discovery speichert Config nicht

### Problem
Nach Klick auf "Automatisch aus Instanz übernehmen" wurden alle Entities gefunden, aber die Config wurde **nicht gespeichert**. Alle Entities blieben `undefined`.

### Root Cause
`_deriveFromController()` hat `renderOnly=true` Parameter verwendet, wodurch das `config-changed` Event **nicht gefeuert** wurde.

### Fix ✅
Config wird nun **automatisch gespeichert** beim Auto-Discovery.

### Nach Update
1. HACS → Frontend → Pool Controller Dashboard → Update auf v1.1.8
2. Karte im Editor öffnen
3. "Automatisch aus Instanz übernehmen" klicken
4. Config wird **automatisch gespeichert** (kein SAVE-Button mehr nötig!)
5. Browser neu laden (F5)

Alle Features sollten nun funktionieren! 🎉
